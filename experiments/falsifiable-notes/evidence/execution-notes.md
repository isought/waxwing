# Execution record

Setup check passed: the exact marker in AGENTS.md was returned without tools, through a CLAUDE.md import. Claude Code 2.1.47, Sonnet 4.6, high effort. All subject checkouts get the same import; only B/C AGENTS.md files get the optional note pointer. This is a documented Claude host adapter, not direct native AGENTS.md loading.

The earlier source rubric was reviewed against the same pinned source before subjects. Exact source-file hashes are retained. Test assertions are source-derived, not runtime results. Review is by the coordinating LLM; no independent human audit time has been incurred or measured.

The author receives only paradigm.md in a clean independent checkout, no task, rubric, previous output, or authoring corrections. Its raw answer is retained before evaluator review. The generator has text tools only; the coordinator saves its final Markdown to a file.

Input efficiency uses total reported input across requests (uncached + cache creation + cache read). This counts repeated context, not unique text. Two observations per condition produce descriptive medians only. Fixed randomization seed 20260914 happened to put both controls first; cache/order confounding must be reported.

No product implementation or application tests. The two Python scripts only run and summarize this experiment.
